{
  "name": "shkills",
  "version": "0.1.0",
  "private": true,
  "description": "Shkills — a central platform to share Claude skills across a company",
  "workspaces": [
    "packages/server",
    "packages/cli",
    "packages/web"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build --workspace=@shkills/cli && npm run build --workspace=@shkills/web && npm run build --workspace=@shkills/server",
    "dev": "npm run dev --workspace=@shkills/server",
    "test": "npm run test --workspace=@shkills/server && npm run test --workspace=@shkills/cli",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "seed": "npm run seed --workspace=@shkills/server",
    "start": "npm run start --workspace=@shkills/server"
  }
}
