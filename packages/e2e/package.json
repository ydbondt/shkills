{
  "name": "@shkills/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "End-to-end acceptance tests — Cucumber over a real browser and the real CLI",
  "scripts": {
    "test:e2e": "node scripts/check-coverage.mjs && NODE_OPTIONS=\"--import tsx\" cucumber-js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^11.3.0",
    "@types/node": "^22.15.0",
    "playwright": "^1.52.0",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
